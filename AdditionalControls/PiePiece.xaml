<UserControl x:Class="AdditionalControls.PiePiece"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdditionalControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Path x:Name="MainPath"
              Stroke="Black"
              StrokeThickness="1"
              Fill="{Binding DefaultBrush}"
              MouseEnter="Path_MouseEnter" MouseLeave="Path_MouseLeave">
        <Path.Data>
            <GeometryGroup>
                <PathGeometry>
                    <PathFigure StartPoint="{Binding Center}" IsFilled="True" x:Name="Geometry" IsClosed="False"/>
                </PathGeometry>
            </GeometryGroup>
        </Path.Data>
        <Path.RenderTransform>
            <TransformGroup>
                <RotateTransform x:Name="SectorRotation" Angle="{Binding }" CenterX="{Binding Center.X}" CenterY="{Binding Center.Y}"/>
            </TransformGroup>
        </Path.RenderTransform>
    </Path>

</UserControl>
